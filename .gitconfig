# This is Git's per-user configuration file.
[user]
    name = Mihails Smolins
    email = mikhail.smolins@gmail.com
[alias]
    st = status
    ci = commit -v
    ssl = log --graph --all
    sl = log --graph --all --oneline
